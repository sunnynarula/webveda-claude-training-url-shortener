"""Acceptance tests, written in RED from each slice's contract in TASKS.md and then frozen.

This folder is a package so that a module here and one in tests/unit/ (the implementer's own
tests) can share a file name without pytest's "import file mismatch" error.
"""
